tcpdump -i any udp dst port 8080 //在所有网卡抓目标8080端口的包；
tcpdump -XX -r saved_file //从已经抓取的包中查看包发送时间IP和包内容；

tcpdump -i eth0 -X -v -s 1514 'udp[40:4] = 0x31323334'

tcpdump -nn -i any -X ether[64:2]=0x2929	//按网卡特定偏移过滤，linux的tcpdump -X时不会打印第一行，故ether还需要算第一行的16个字节(即算偏移时tcpdump数出来的位置加16)。自测tcpdump抓IPV6的包使用udp的偏移不准确，使用ether的偏移准确，wireshark是两个均正确。

tcpdump -G 15 -W 1 -w myfile -i eth0 'port 8080'	//每过15秒生成一个文件，-G {sec} (rotate dump files every x seconds) and -W {count} (limit # of dump files) 


tcpdump -nn	可以立即显示抓取信息
tcpdump -X	显示包内容，虽然是每两个字节一个空格地显示，但却是按网络序严格排序，即最小网络序单位是字节；（与hexdump不同，hexdump默认输出格式的最小显示单位是short），tcpdump的输出类似hexdump -C